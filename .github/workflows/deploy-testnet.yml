name: Deploy Testnet

on:
  push:
    branches:
      - dev
    paths:
      - 'contracts/**'
      - 'Clarinet.toml'
      - 'settings/Testnet.toml'
      - 'deployments/**'
  workflow_dispatch:
    inputs:
      dry_run:
        description: 'Dry run (generate plan only, no deploy)'
        type: boolean
        default: true

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Install Clarinet
        run: |
          set -euo pipefail
          VERSION="v3.21.0"
          curl -sSL -o clarinet.tar.gz "https://github.com/stx-labs/clarinet/releases/download/${VERSION}/clarinet-linux-x64-glibc.tar.gz"
          tar -xzf clarinet.tar.gz clarinet
          chmod +x clarinet
          sudo mv clarinet /usr/local/bin/clarinet
          clarinet --version

      - name: Clarinet Check
        run: clarinet check --manifest-path Clarinet.toml

      - name: Run Full Test Suite
        run: npm run ci

  deploy-testnet:
    needs: validate
    runs-on: ubuntu-latest
    environment: testnet
    steps:
      - uses: actions/checkout@v7

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Install Clarinet
        run: |
          set -euo pipefail
          VERSION="v3.21.0"
          curl -sSL -o clarinet.tar.gz "https://github.com/stx-labs/clarinet/releases/download/${VERSION}/clarinet-linux-x64-glibc.tar.gz"
          tar -xzf clarinet.tar.gz clarinet
          chmod +x clarinet
          sudo mv clarinet /usr/local/bin/clarinet
          clarinet --version

      - name: Generate Testnet Deployment Plan
        run: |
          clarinet deployment generate \
            --manifest-path Clarinet.toml \
            --testnet \
            --low-cost \
            --plan-path deployments/default.testnet-plan.yaml

      - name: Deploy to Testnet
        if: ${{ !inputs.dry_run }}
        env:
          DEPLOYER_MNEMONIC: ${{ secrets.TESTNET_DEPLOYER_MNEMONIC }}
        run: |
          clarinet deployment apply \
            --manifest-path Clarinet.toml \
            --plan-path deployments/default.testnet-plan.yaml \
            --testnet

      - name: Smoke Test Deployed Contracts
        if: ${{ !inputs.dry_run }}
        run: |
          echo "Running post-deployment smoke tests..."
          npx vitest run tests/sanity.test.ts tests/check-compile.test.ts --config vitest.config.ts --pool=threads

      - name: Generate Deployment Report
        if: always() && !inputs.dry_run
        run: |
          echo "## Testnet Deployment Report" >> $GITHUB_STEP_SUMMARY
          echo "**Deployer:** ST1BK6TFDEJ4TBVWH5SHNB6SPNWGY06YZFG9WMM4P" >> $GITHUB_STEP_SUMMARY
          echo "**Network:** Testnet" >> $GITHUB_STEP_SUMMARY
          echo "**Plan:** deployments/default.testnet-plan.yaml" >> $GITHUB_STEP_SUMMARY

      - name: Upload Deployment Plan Artifact
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: testnet-deployment-plan
          path: deployments/default.testnet-plan.yaml
