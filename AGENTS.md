# Conxian AGENTS.md

## BOS Operational Standards
Clarity-version:4

### Sovereign-First Deployment Mandate
All Conxian core contracts must use dynamic principals fetched via `operational-treasury.clar`. Any hardcoded `ST...` or `SP...` addresses in production contract source code are considered a build-break. Jules must flag and fix these during the planning phase.

### Zero Secret Egress (ZSE) Compliance
- Sensitive operational logic and private configurations must remain in Linear or Supabase.
- Code should only contain "State Proof" logic and public-facing protocol primitives.
- Functional stubs in production paths must return explicit service errors (501/503) and fail closed.

### BitVM2 Integration
BitVM2 integration must use SNARK-based state proofs verified via `lib-conxian-core`. Verification logic must bridge Bitcoin L1 state and the BitVM2 verification engine as per CJCS v2.0.

### Repository Hygiene
- Submodule drift is monitored by the CI Contamination Guard.
- Submodule pins must be updated immediately upon validated remediation.
- No launch-critical automation may depend on personal/bootstrap wallets.

---

## Clarity 4 Compliance Mandate (Updated 2026-04-23)

### Core Rule
- **clarity-version = 4** is MANDATORY for ALL contracts. Versions 1, 2, 3 are BANNED.
- **epoch = "latest"** must be set for every contract in Clarinet.toml.
- All 207 contracts in the project use clarity-version: 4 with epoch: latest.

### Breaking Changes Enforced (Clarity 4)

1. **`to-consensus-buff?` REMOVED** — all on-chain serialization must be eliminated:
   - [FIXED] `cross-chain/bridge-nft.clar` — nonce now serves as pre-computed payload hash; verified via `secp256k1-verify` directly.
   - [FIXED] `yield/yield-optimizer.clar` (line 65) — removed `to-consensus-buff?` call from `execute-yield-intent`.
   - [FIXED] `agents/payment-forge.clar` (line 26) — removed `to-consensus-buff?` call from `trigger-x402-settlement`; streamlined `let` block.
   - [VERIFIED] `utils/encoding.clar` — commented-out wrapper references only.

2. **`stacks-block-height` replaces bare `block-height`** in all contexts except `burn-block-height` and `get-block-info?`:
   - [FIXED] 15 files patched via bulk regex: `agent-risk.clar`, `autonomous-registry.clar`, `block-utils.clar`, `conxian-protocol.clar`, `cxd-treasury.clar`, `dex-factory.clar`, `dimensional-core.clar`, `jurisdictional-sharding.clar`, `pyth-oracle-adapter.clar`, `redstone-oracle-adapter.clar`, `sab-election.clar`, `swap-router.clar`, `test-c4-helper.clar`, `voting.clar`.

3. **`get-block-info? time block-height` → `get-block-info? time stacks-block-height`**:
   - [FIXED] `compliance/jurisdictional-sharding.clar:204`.

### Build System — Clarinet.toml
- Generated by `stacksorbit/rebuild_toml.py` — scans all 210+ .clar files for `contract-call?`, `impl-trait`, `use-trait`, `depends_on` declarations.
- 207 contracts registered, each with `clarity_version = 4` and `epoch = "latest"`.
- Deployment plans (simnet/mainnet) are generated by `clarinet check`.

### Key Contracts Registry
| Contract | Purpose | Status |
|---|---|---|
| `cross-chain/bridge-nft.clar` | CCTP/NTT Bridge | FIXED (Clarity 4) |
| `yield/yield-optimizer.clar` | Yield Engine | FIXED (Clarity 4) |
| `agents/payment-forge.clar` | ISO 20022/x402 Settlement | FIXED (Clarity 4) |
| `compliance/jurisdictional-sharding.clar` | Jurisdiction Sharding | FIXED (block-height) |
| `utils/block-utils.clar` | Block Utilities | FIXED (block-height) |

### Pending Work
- Run `clarinet check` to verify all 207+ contracts compile cleanly.
- Resolve any remaining compilation errors after bulk fixes.
- Full testnet deployment with Phase 7 contracts.
- Comprehensive contract audit for business logic, security, gas optimization.

### Tooling
- `stacksorbit/rebuild_toml.py` — auto-generates Clarinet.toml from contract scanning.
- Run from project root: `cd stacksorbit && pip install -r requirements.txt && python rebuild_toml.py`
- Stacks CLI and Clarinet 3.12.0+ required.
