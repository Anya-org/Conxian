;; Conxian TWAP Oracle v2 Complex (minimal safe stub)

(define-read-only (get-twap (pair {base: principal, quote: principal}) (window uint))
  (ok u0))

(define-read-only (supports-window (window uint))
  true)
